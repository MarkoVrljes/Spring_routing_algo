# Set default behavior, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always normalize and ensure LF endings
*.sh text eol=lf
*.java text
*.js text
*.css text
*.html text
README.md text

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
